# =============================================================================
# SILNI APP ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# IMPORTANT: Never commit .env to version control!
#
# After modifying .env, regenerate the configuration with:
#   flutter pub run build_runner build --delete-conflicting-outputs
# =============================================================================

# =============================================================================
# APP ENVIRONMENT
# =============================================================================
# APP_ENV: Which Supabase instance to use
#   - staging: Development/testing (SUPABASE_STAGING_*)
#   - production: Production environment (SUPABASE_PRODUCTION_*)
APP_ENV=staging

# ENVIRONMENT: Sentry environment for error tracking
#   - development: Local development (errors not sent to Sentry)
#   - staging: TestFlight/staging builds
#   - production: Production builds
ENVIRONMENT=development

# IS_TESTFLIGHT: Enable for TestFlight builds (enables remote error logging)
IS_TESTFLIGHT=false

# =============================================================================
# SUPABASE - STAGING
# =============================================================================
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
SUPABASE_STAGING_URL=https://your-staging-project.supabase.co
SUPABASE_STAGING_ANON_KEY=your-staging-anon-key

# =============================================================================
# SUPABASE - PRODUCTION
# =============================================================================
# Get these from your production Supabase project
SUPABASE_PRODUCTION_URL=https://your-production-project.supabase.co
SUPABASE_PRODUCTION_ANON_KEY=your-production-anon-key

# =============================================================================
# ERROR TRACKING (SENTRY)
# =============================================================================
# Get DSN from: https://sentry.io/settings/YOUR_ORG/projects/silni/keys/
SENTRY_DSN=https://your-key@sentry.io/your-project-id

# =============================================================================
# CLOUDINARY (OPTIONAL - for future image processing)
# =============================================================================
# Get these from: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
CLOUDINARY_UPLOAD_PRESET=

# =============================================================================
# FIREBASE WEB (LEGACY - for potential future web support)
# =============================================================================
# These are public API keys from Firebase Console
# Get from: https://console.firebase.google.com/project/YOUR_PROJECT/settings/general
FIREBASE_API_KEY=
FIREBASE_AUTH_DOMAIN=
FIREBASE_PROJECT_ID=
FIREBASE_STORAGE_BUCKET=
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_APP_ID=
FIREBASE_MEASUREMENT_ID=

# =============================================================================
# EDGE FUNCTION SECRETS (DO NOT ADD HERE!)
# =============================================================================
# The following secrets must be set via Supabase CLI, NOT in this file:
#
#   supabase secrets set FIREBASE_SERVICE_ACCOUNT='<json>'
#   supabase secrets set SERVICE_ROLE_JWT='<jwt_token>'
#
# These are server-side only and should NEVER be in client code.
# =============================================================================
