---
name: Phase 8 — Consolidated cleanup (version bump + content audit closures)
description: Five tasks. iOS bundle bumped 2.0.1+38 → 2.1.0+1, hadith row 103 grade corrected to صحيح to match the new Ibn Hibban citation, V1_1_BACKLOG.md committed for tracking deferred items, full baseline verified green.
type: project
---

# PHASE 8 — Consolidated Cleanup

**Date:** 2026-04-27
**Status:** ✅ All 5 tasks shipped. App is at version 2.1.0+1; pending v1.0 audit items closed or formally deferred to v1.1.
**Commit:** `<this commit>`.

---

## Task 1 — iOS bundle version bump ✅

| Surface | Before | After |
|---|---|---|
| `pubspec.yaml` line 4 | `version: 2.0.1+38` | `version: 2.1.0+1` |
| `build/ios/iphoneos/Runner.app/Info.plist` `CFBundleShortVersionString` | (stale) | `2.1.0` (PlistBuddy-verified) |
| `build/ios/iphoneos/Runner.app/Info.plist` `CFBundleVersion` | (stale) | `1` (PlistBuddy-verified) |

### Steps executed

1. Edited `pubspec.yaml:4`. Single line change.
2. Grepped for hardcoded `2.0.1` references in `lib/` `ios/` `android/` `pubspec.yaml`. The only true consumer was `ios/Flutter/Generated.xcconfig` (auto-regenerated by `flutter build` — confirmed by the post-build PlistBuddy check). Other matches in `pubspec.yaml` were package version constraints (`animations: ^2.0.11`, `in_app_review: ^2.0.10`, `flutter_svg: ^2.0.16`) — false positives.
3. `flutter clean` (deleted Generated.xcconfig + ephemeral artifacts).
4. `flutter pub get` (139 dependency-update notices, all incompatible with current constraints — noise, not actionable).
5. `flutter build ios --release --no-codesign` — succeeded (70.0 MB Runner.app, 112.1 s Xcode phase).
6. PlistBuddy verification:
   ```
   /usr/libexec/PlistBuddy -c "Print CFBundleShortVersionString" build/ios/iphoneos/Runner.app/Info.plist
   2.1.0
   /usr/libexec/PlistBuddy -c "Print CFBundleVersion" build/ios/iphoneos/Runner.app/Info.plist
   1
   ```

Auto-propagation worked. No manual Info.plist or project.pbxproj edits needed (per CTO standing order).

---

## Task 2 — Hadith row 103 grade correction ✅

### Migration

[supabase/migrations/20260428400000_hadith_row_103_grade_correction.sql](supabase/migrations/20260428400000_hadith_row_103_grade_correction.sql)

Defensive UPDATE matching on `(display_priority=103 AND source='صحيح ابن حبان ٤٤٠' AND grade='حسن')` — idempotent (re-runs match zero rows after fill). Self-verifies `grade='صحيح'` AND `source='صحيح ابن حبان ٤٤٠'` post-update; aborts the transaction on either mismatch. Arabic comment block documents the rationale (al-Albani's verification of Ibn Hibban 440), founder + CTO approval date, and the Wave-2-era trail.

### Pre-state (MCP-introspected)

```
display_priority=103: source='صحيح ابن حبان ٤٤٠' narrator='أبو بكرة' grade='حسن'
```

### Post-state (MCP-verified after `supabase db push`)

```
display_priority=103: source='صحيح ابن حبان ٤٤٠' narrator='أبو بكرة' grade='صحيح'
```

Migration applied without `RAISE`. Self-verification block passed. ✅

---

## Task 3 — V1_1_BACKLOG.md ✅

[V1_1_BACKLOG.md](V1_1_BACKLOG.md) created in repo root. Documents items deferred from v1.0 audits across:

- **Database** — Wave 2.6 (delete_user_account teardown removal), Wave 2.7 (RLS policy dedup), Wave 3 (FK target unification).
- **Performance** — 10 🟡 + 10 🟢 findings with revisit triggers (100 / 1000 user thresholds).
- **Content** — 3 unnumbered legacy hadith rows (display_priority 94/95/96), 16 hardcoded copy candidates for admin-table migration.
- **UX** — ~24 🟡 + ~20 🟢 from UX_FLOW_AUDIT.md not closed in Phase 5.
- **Notification topic infrastructure** — the v1.1 plan that supersedes the cosmetic toggles cut in Phase 5.5.
- **Phone-invite subsystem** — cut in Phase 5; re-introduce only on real-user demand (also fixes the dormant `r.name` column-mismatch bug).
- **Android one-tap notification settings** — `app_settings` package addition.
- **`admin_ai_identity` defensive seed** — currently empty on prod; seed when admin panel gains the editor.
- **Audit surfaces not yet fired** — security/PII, accessibility.

Pure documentation, no code change.

---

## Task 4 — Final verification ✅

| Check | Result | Verdict |
|---|---|---|
| `flutter analyze` | 6 issues (same lines: 1 warn `_saveFamilyName`, 5 info `unnecessary_underscores`) | ✅ baseline preserved |
| `flutter test test/unit/` | 1354 / 0 | ✅ |
| `flutter test test/golden/` | 8 / 0 | ✅ |
| `bash scripts/check_migrations_for_missing_on_delete.sh --diff-only origin/main` | 16 migration files checked, 4 contain user-id FK references, all explicit about ON DELETE | ✅ clean |
| `flutter build ios --release --no-codesign` | succeeded (70.0 MB, 112.1 s) | ✅ |
| PlistBuddy `CFBundleShortVersionString` | `2.1.0` | ✅ |
| PlistBuddy `CFBundleVersion` | `1` | ✅ |
| `supabase db push` (Task 2 migration) | applied cleanly; self-verify passed | ✅ |
| MCP post-apply row 103 | `grade='صحيح'`, `source='صحيح ابن حبان ٤٤٠'`, `narrator='أبو بكرة'` | ✅ |

No regressions. No baseline drift.

---

## Surprises

1. **Three `2.0.1` substring matches in `pubspec.yaml` were package-version constraints**, not the app version (animations/in_app_review/flutter_svg all happen to use the digit pattern). Confirmed false positives by inspection — none are actual version-string consumers. The grep filter would catch any genuine drift if a future contributor accidentally hardcoded the app version into Dart.
2. **`flutter pub get` reported 139 packages with newer versions blocked by constraints + 2 discontinued.** Noise only — no actionable failures, no SDK incompatibility. Worth a v1.1 dependency-refresh session but doesn't gate anything today.
3. **The version-propagation chain worked cleanly** through the documented path (`pubspec.yaml` → `Generated.xcconfig` → `Info.plist`). PlistBuddy returned exactly the expected `2.1.0` / `1`. No drift, no manual edit needed.
4. **Build time jumped from prior phases' ~50s to 112s.** Expected — `flutter clean` purged the build cache. First post-clean build always full-recompiles. Subsequent incremental builds will return to ~30-50s.

---

## Open questions for the CTO

None for this session — all five tasks landed cleanly, no halt-condition triggered, no edge cases discovered.

The standing v1.1 questions (notification topic infra, phone-invite re-introduction, Android one-tap settings, the 3 unnumbered hadith rows, the 16 hardcoded copy candidates) are all tracked in [V1_1_BACKLOG.md](V1_1_BACKLOG.md). Recommend the founder do one read-through of that file when convenient to confirm the deferred-items list matches their mental model.

---

## TestFlight readiness — yes, blocker cleared

App version is now `2.1.0+1`, which is higher than the App Store Connect "approved" entry of `2.0.1`. Founder can run the signed `flutter build ipa` + upload step. The previously-rejected validation errors should not recur:

- `Invalid Pre-Release Train. The train version '2.0.1' is closed for new build submissions.` → resolved by minor-version bump.
- `CFBundleShortVersionString [2.0.1] must contain a higher version than the previously approved version [2.0.1].` → resolved; new value `2.1.0` is strictly greater.

All v1.0 audit items closed or formally deferred. The codebase is in launch-ready state.
